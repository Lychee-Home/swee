# Release-please migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace `scripts/compute-version-bump.sh` with `googleapis/release-please-action`, and
gate both deploy and release behind a deliberately-merged Release PR instead of every push to
`main`.

**Architecture:** `.github/workflows/ci.yml` gains a `release-please` job that runs on every push
to `main`, maintaining a standing Release PR (with `CHANGELOG.md`) via release-please's own
Conventional Commits parsing. The existing `deploy` job is retargeted to depend on that job's
`release_created` output, so it only fires on the run where the Release PR itself gets merged.

**Tech Stack:** GitHub Actions, `googleapis/release-please-action@v4` (release type `simple`),
Node/npx (for local JSON/YAML validation only — the action itself needs no repo-side npm setup).

## Global Constraints

- Repo: `Lychee-Home/swee` (used for any repo-URL references).
- Seed version: `2.4.0` (matches current `v2.4.0` tag) — the first Release PR must compute its
  bump from this, not from zero.
- Release type `simple` (no `pyproject.toml`/`setup.py` version field in this repo).
- `CHANGELOG.md` is generated by release-please (per approved spec) — do not hand-write it.
- Never push directly to `main` — this work happens on branch `release-please-migration`
  (already created locally with the spec commit) and ships via PR.
- No new test harness is introduced (per `CLAUDE.md`, this repo has no Discord-command-layer test
  harness); validation here means JSON/YAML syntax checks plus a documented manual verification
  checklist for the parts that only a real GitHub Actions run can exercise.

---

### Task 1: Add release-please config files

**Files:**
- Create: `release-please-config.json`
- Create: `.release-please-manifest.json`

**Interfaces:**
- Produces: the config/manifest pair that `googleapis/release-please-action@v4` (added in Task 2)
  reads by default from the repo root — no `config-file`/`manifest-file` inputs needed on the
  action since these are its default filenames.

- [ ] **Step 1: Create `release-please-config.json`**

```json
{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "release-type": "simple",
  "packages": {
    ".": {
      "changelog-path": "CHANGELOG.md"
    }
  }
}
```

- [ ] **Step 2: Validate it's well-formed JSON**

Run: `node -e "JSON.parse(require('fs').readFileSync('release-please-config.json','utf8')); console.log('ok')"`
Expected: `ok`

- [ ] **Step 3: Create `.release-please-manifest.json`, seeded at the current version**

```json
{
  ".": "2.4.0"
}
```

- [ ] **Step 4: Validate it's well-formed JSON**

Run: `node -e "JSON.parse(require('fs').readFileSync('.release-please-manifest.json','utf8')); console.log('ok')"`
Expected: `ok`

- [ ] **Step 5: Commit**

```bash
git add release-please-config.json .release-please-manifest.json
git commit -m "chore: add release-please config seeded at 2.4.0"
```

---

### Task 2: Restructure ci.yml to gate deploy behind release-please

**Files:**
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `release-please-config.json` / `.release-please-manifest.json` from Task 1 (read
  implicitly by the action — no explicit reference needed in the workflow YAML).
- Produces: job id `release-please` with output `release_created` (`'true'`/`'false'` string),
  consumed by the `deploy` job's `if:` condition in this same task.

- [ ] **Step 1: Replace the full contents of `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  push:
    branches: [main]

jobs:
  release-please:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  deploy:
    needs: release-please
    if: needs.release-please.outputs.release_created == 'true'
    runs-on: self-hosted
    steps:
      - name: Pull latest and restart service
        env:
          SWEE_DIR: ${{ vars.SWEE_DIR }}
          SWEE_USER: ${{ vars.SWEE_USER }}
        run: |
          set -euo pipefail
          : "${SWEE_DIR:?SWEE_DIR repo variable must be set}"
          if [ -n "${SWEE_USER:-}" ]; then
            sudo -u "$SWEE_USER" -H "$SWEE_DIR/deploy/ci-deploy.sh"
          else
            cd "$SWEE_DIR"
            git pull --ff-only origin main
            .venv/bin/pip install -q -r requirements.txt
          fi
          sudo systemctl restart swee
```

This removes: the `paths` filter (release-please needs to see every commit for accurate
changelog bookkeeping — see spec's "Removing the paths filter" section), the old `Determine
release` step (`scripts/compute-version-bump.sh`), and the old `Create tag and release` step —
`googleapis/release-please-action` replaces all three.

- [ ] **Step 2: Validate the YAML parses**

Run: `npx -y yaml-lint .github/workflows/ci.yml`
Expected: no output (or an explicit "valid" message) and exit code 0 — no syntax errors reported.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: gate deploy and release behind release-please"
```

---

### Task 3: Remove the old version-bump script and its tests

**Files:**
- Delete: `scripts/compute-version-bump.sh`
- Delete: `scripts/test-compute-version-bump.sh`

**Interfaces:**
- Consumes: confirmation from Task 2 that `ci.yml` no longer references either script.

- [ ] **Step 1: Confirm nothing still references these scripts**

Run: `grep -rn "compute-version-bump" --include="*.yml" --include="*.sh" --include="*.md" .`
Expected: only matches inside `docs/superpowers/specs/2026-07-10-release-versioning-design.md`,
`docs/superpowers/plans/2026-07-10-release-versioning.md`, and
`docs/superpowers/specs/2026-07-16-release-please-migration-design.md` (historical spec/plan
docs, left as-is as project history) — no matches in `.github/workflows/ci.yml`, `README.md`, or
`docs/deployment.md` (the latter two get fixed in Task 4).

- [ ] **Step 2: Delete both scripts**

```bash
git rm scripts/compute-version-bump.sh scripts/test-compute-version-bump.sh
```

- [ ] **Step 3: Commit**

```bash
git commit -m "chore: remove version-bump script, replaced by release-please"
```

---

### Task 4: Update docs to describe the gated flow

**Files:**
- Modify: `docs/deployment.md`
- Modify: `README.md:139-140`

**Interfaces:**
- Consumes: the final job names/behavior from Task 2 (`release-please`, gated `deploy`).

- [ ] **Step 1: Rewrite the "Continuous deployment" section of `docs/deployment.md`**

Replace the existing "Continuous deployment" section (currently describing a separate
`deploy.yml`, which doesn't match the actual single `ci.yml` file) with:

```markdown
## Continuous deployment

`.github/workflows/ci.yml` runs on every push to `main`. Its `release-please` job (see
Versioning below) determines whether that push produced a new release; only when it did does
the `deploy` job run, on a self-hosted GitHub Actions runner installed on the same host as the
bot (see [GitHub's
docs](https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/add-runners)
for installing the runner itself). Merging an ordinary feature PR to `main` does **not** deploy
by itself — see Versioning below for what does. When `deploy` runs, it `cd`s into the deployed
repo, specified by the **required** `SWEE_DIR` repo variable (the canonical absolute path to the
deployed repo — no trailing slash or symlinks, matching what `deploy/setup.sh` resolves via
`cd ... && pwd`), does a `git pull --ff-only`, reinstalls dependencies, and restarts
`swee.service`. No inbound access to the host is required since the runner polls GitHub
outbound; `deploy/setup.sh` installs the passwordless-sudo rule (`systemctl restart swee`) the
workflow needs to restart the service non-interactively.

By default the runner is assumed to run as the same OS user `swee` itself runs as (matching
`deploy/setup.sh`'s default). To run the GitHub Actions runner as a separate, dedicated OS user instead
(recommended once this host runs apps beyond this one) — so the runner never needs direct file access to
any app's directory — set both of the following together:

- Run `deploy/setup.sh` with `RUNNER_USER=<runner's OS user>` set, e.g.
  `RUNNER_USER=github-runner ./deploy/setup.sh`. This grants `<runner's OS user>` passwordless sudo to run
  `deploy/ci-deploy.sh` as `swee`'s own OS user, and to restart `swee.service`.
- Set the `SWEE_USER` repo variable (alongside the existing `SWEE_DIR`) to `swee`'s own OS user. This
  tells `ci.yml` to delegate deploys via `sudo -u "$SWEE_USER" -H "$SWEE_DIR/deploy/ci-deploy.sh"` instead of running
  `git pull`/`pip install` directly.

Leave both unset to keep the current single-user behavior, where the runner's own OS user must already
have write access to `$SWEE_DIR`. `swee.service`'s own `User=` and the bot's own Palworld-restart sudo
rule are unaffected either way — only the CI identity changes.
```

- [ ] **Step 2: Rewrite the "Versioning" section of `docs/deployment.md`**

```markdown
## Versioning

Releases are managed by [`release-please`](https://github.com/googleapis/release-please) via
the `release-please` job in `.github/workflows/ci.yml`, using Conventional Commits
(`feat: ...`, `fix: ...`, `chore: ...`, etc.) parsed from commits on `main`.

On every push to `main`, release-please updates a standing **Release PR** (title like
`chore(main): release X.Y.Z`) with the accumulated version bump and `CHANGELOG.md` entries:
`feat` bumps MINOR, `fix`/`perf` bump PATCH, and a `!` after the type/scope (or a
`BREAKING CHANGE:` footer starting its own line in the commit body) bumps MAJOR. Any other type
(`docs`, `chore`, `ci`, `style`, `test`, `refactor`, `build`, `revert`) doesn't contribute a
version bump, though it may still appear in the changelog depending on release-please's default
section mapping.

**Nothing ships until you merge that Release PR.** Merging it is what tags the release, publishes
the GitHub Release, and — via the `deploy` job's dependency on `release_created` — triggers the
actual deploy. Ordinary feature PRs merging to `main` only update the Release PR's diff; they
don't deploy or release anything by themselves.

Reserve `!`/`BREAKING CHANGE:` for changes that break an existing deployment on upgrade — e.g. a
new required `.env` var, a removed/renamed slash command, a changed REST config shape.
```

- [ ] **Step 3: Update `README.md:139-140`**

Find:
```markdown
Pushes to `main` auto-deploy to this running instance, and releases are tagged automatically from
PR titles — see [`docs/deployment.md`](docs/deployment.md) for the full CI/CD and versioning
```

Replace with:
```markdown
Pushes to `main` update a standing release-please Release PR; merging *that* PR is what deploys
this running instance and tags a release — see [`docs/deployment.md`](docs/deployment.md) for the
full CI/CD and versioning
```

- [ ] **Step 4: Commit**

```bash
git add docs/deployment.md README.md
git commit -m "docs: describe gated release-please deploy/release flow"
```

---

### Task 5: Manual verification checklist (post-PR-merge)

**Files:** none — this task is a verification checklist, not a code change. No automated test
harness can exercise a real GitHub Actions run outside GitHub's own infrastructure, so this is
run against the actual repo after the PR from this plan is merged.

**Interfaces:**
- Consumes: the merged state of Tasks 1–4.

- [ ] **Step 1: Confirm the workflow file is valid from GitHub's own perspective**

After pushing the branch, open the PR in GitHub and check the "Actions" tab / PR checks — GitHub
parses `ci.yml` on push regardless of whether the `release-please` job's trigger conditions are
met, so a YAML/schema error surfaces immediately as a failed or skipped run rather than silently.

- [ ] **Step 2: Confirm a feature-shaped push does not deploy**

Once this PR merges to `main` (a `chore`-shaped commit, per Conventional Commits — the PR title
must start with `chore:` or `docs:` since this is infra/docs work, not a feature or fix), check
the Actions run for that push: `release-please` should run and either open or update a Release
PR, `release_created` should be `false`, and the `deploy` job should show as **skipped** (not
run) in the workflow run summary.

- [ ] **Step 3: Confirm the Release PR reflects the seeded version**

Open the Release PR release-please created/updated on `main`. Its title/diff should propose the
*next* version after `2.4.0` (e.g. `2.4.1` for a `fix`, `2.5.0` for a `feat` merged since this
change landed) — not `0.1.0` or `1.0.0`. If it proposes starting from zero, the manifest seed in
Task 1 didn't take effect; re-check `.release-please-manifest.json` is at repo root and its key
is exactly `"."`.

- [ ] **Step 4: Confirm merging the Release PR deploys**

At the next point a real release is warranted, merge the Release PR and confirm in the Actions
run: `release-please` reports `release_created: true`, a new `vX.Y.Z` tag and GitHub Release
exist, `CHANGELOG.md` was updated in the merge, and the `deploy` job actually ran (not skipped)
and restarted `swee.service` successfully.

No commit for this task — it's a checklist run against the live repo, not a code change.
